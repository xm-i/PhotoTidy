# Copilot Instructions

## プロジェクト ガイドライン
- In this repository, prefer using the locally added R3.JsonConfig solution projects for configuration persistence instead of ad-hoc/manual JSON DTO implementations.